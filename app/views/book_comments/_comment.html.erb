      <table>
        <thead>
          <tr>
            <th></th>
            <th>コメント投稿者</th>
            <th>コメント内容</th>
          </tr>
        </thead>

        <tbody>
          <tr>
            <td>
              <%= image_tag comment.user.get_profile_image, size:"50x50" %><br>
              <%= link_to comment.user.name, (comment.user) %>
            </td>
            <td><%= comment.comment %></td>
          <% if comment.user == current_user %>
            <td><%= link_to "削除", book_book_comment_path(book, comment), method: :delete, remote: true %></td>
          <% end %>
          </tr>
        </tbody>
      </table>


