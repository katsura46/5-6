 <table>
    <tr>
      <td>
        <%= form_with(model: [book, comment]) do |f| %>
          <%= f.text_area :comment, size: "100x5" %>
          <%= f.submit '送信' %>
        <% end %>
      </td>
    </tr>
  </table>
